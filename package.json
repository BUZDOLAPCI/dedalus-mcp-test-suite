{
  "name": "dedalus-mcp-test-suite",
  "version": "1.0.0",
  "description": "End-to-end test suite for Dedalus MCP servers",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node dist/index.js",
    "test:watch": "tsc --watch",
    "lint": "eslint src --ext .ts"
  },
  "keywords": [
    "dedalus",
    "mcp",
    "test-suite",
    "model-context-protocol"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "dedalus-labs": "^0.1.0-alpha.8",
    "dotenv": "^16.4.0",
    "chalk": "^5.3.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}
